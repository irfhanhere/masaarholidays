import { ComingSoon } from "@/components/admin/ComingSoon";

export const metadata = { title: "WhatsApp Templates | Masaar Admin", robots: { index: false } };

export default function WhatsAppTemplatesPage() {
  return (
    <ComingSoon
      title="WhatsApp Templates"
      description="Edit the pre-filled WhatsApp messages used across the site without a developer."
      inspirationFile="ADMIN-WHATSAPP TEMPLATES.png"
    />
  );
}
